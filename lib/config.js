'use strict'

// Default configuration.
const config = new Map([
  ['api', 'api.stackexchange.com'],
  ['protocol', 'https:'],
  ['site', 'stackoverflow'],
  ['version', '2.2']
])

// Expose config
module.exports = config
